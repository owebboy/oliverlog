{
  "name": "oliverlog",
  "version": "1.0.0",
  "description": "a log of events from oliver pope",
  "main": "index.md",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Oliver Pope",
  "license": "MIT",
  "devDependencies": {
    "git-scripts": "^0.2.1",
    "surge": "^0.19.0"
  },
  "git": {
    "scripts": {
      "pre-push": "harp compile; surge --project ./www"
    }
  }
}
